Tiger-Tail Microblog has been deployed!

{{- if .Values.ingress.enabled }}
Access the application via Ingress:
{{- range $host := .Values.ingress.hosts }}
  {{- range .paths }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ $host.host }}{{ .path }}
  {{- end }}
{{- end }}
{{- else }}
Access the application by port-forwarding:
  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "tiger-tail-chart.fullname" . }} 8080:{{ .Values.service.port }}
  Then visit: http://localhost:8080
{{- end }}

{{- if .Values.postgresql.enabled }}
PostgreSQL is enabled at {{ include "tiger-tail-chart.fullname" . }}-postgresql:{{ .Values.postgresql.primary.service.port }}
{{- end }}

{{- if .Values.redis.enabled }}
Redis is enabled at {{ include "tiger-tail-chart.fullname" . }}-redis-master:{{ .Values.redis.master.service.port }}
{{- end }}

For more information and connection details, see the chart README.
