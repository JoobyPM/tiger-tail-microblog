apiVersion: v1
kind: Service
metadata:
  name: {{ include "tiger-tail-chart.fullname" . }}
  labels:
    {{- include "tiger-tail-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      protocol: TCP
      targetPort: http
  selector:
    {{- include "tiger-tail-chart.selectorLabels" . | nindent 4 }}
